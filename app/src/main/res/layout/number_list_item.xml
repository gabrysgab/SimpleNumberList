<?xml version="1.0" encoding="utf-8"?>

<androidx.cardview.widget.CardView
        xmlns:card_view="http://schemas.android.com/apk/res-auto"
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools" android:layout_margin="8dp"
        android:id="@+id/card_view"
        android:layout_gravity="center"
        android:layout_width="match_parent"
        android:layout_height="@dimen/number_item_height"
        card_view:cardCornerRadius="@dimen/number_item_card_corner_radius">

    <TextView
            android:textStyle="bold"
            android:gravity="center"
            android:id="@+id/number_text"
            android:layout_width="match_parent" android:layout_height="match_parent"
            android:textSize="@dimen/number_item_text_size"
            tools:text="2"/>

</androidx.cardview.widget.CardView>

